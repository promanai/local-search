{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "LocalSearch main window with no direct plugin capabilities",
  "windows": ["main"],
  "permissions": ["core:default"]
}
