[package]
name = "localsearch-platform-core"
description = "Platform-neutral operating-system adapter contracts for LocalSearch"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
localsearch-core = { path = "../core" }
serde.workspace = true
serde_json = { version = "1.0.151", optional = true }
thiserror.workspace = true

[dev-dependencies]
serde_json = "1.0.151"

[features]
provider-contract-testing = ["dep:serde_json"]

[lints]
workspace = true
