[package]
name = "localsearch-filesystem-graph"
description = "Durable platform-neutral SQLite filesystem graph for LocalSearch"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
localsearch-core = { path = "../core" }
localsearch-benchmark-data = { path = "../benchmark-data" }
localsearch-platform-core = { path = "../platform-core" }
rusqlite.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2 = "0.10.9"
thiserror.workspace = true

[target.'cfg(not(target_arch = "aarch64"))'.dependencies]
rusqlite = { workspace = true, features = ["bundled"] }

[dev-dependencies]
tempfile = "3.23.0"

[lints]
workspace = true
